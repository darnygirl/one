---
/**
 * Modern AI Chat Interface
 *
 * Production-ready chat with:
 * - Centered prompt UI (transitions to bottom after first message)
 * - Free tier: Gemini Flash Lite (no API key needed)
 * - Premium tier: GPT-4, Claude, and 50+ models with OpenRouter API key
 * - Generative UI components (charts, tables, forms, timelines)
 * - Clean, maintainable codebase
 */

import Layout from '@/layouts/Layout.astro';
import { ChatClient } from '@/components/chat';

const title = 'AI Chat - ChatGPT Competitor';
const description = 'Free AI chat with centered prompt UI, 15+ AI models, and generative UI. 10x faster than ChatGPT.';
---

<Layout
  title={title}
  description={description}
  sidebarInitialCollapsed={true}
  hideHeader={true}
  fullHeight={true}
>
  <ChatClient client:only="react" />
</Layout>
